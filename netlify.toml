# Fix for Decap CMS admin routing
[[headers]]
  for = "/admin/*"
  [headers.values]
    X-Frame-Options = "DENY"
    X-XSS-Protection = "1; mode=block"

# Ensure admin/index.html is served correctly
[[redirects]]
  from = "/admin"
  to = "/admin/index.html"
  status = 200
  force = true
